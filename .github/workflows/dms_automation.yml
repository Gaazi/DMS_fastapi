# name: "DMS: Automated Testing & cPanel Deployment"

# on:
#   push:
#     branches: [ "main", "dev" ] # ان دونوں برانچز پر ایکشن چلے گا

# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#     - uses: actions/checkout@v4
    
#     - name: Set up Python
#       uses: actions/setup-python@v4
#       with:
#         python-version: '3.11'
        
#     - name: Install Dependencies
#       run: |
#         python -m pip install --upgrade pip
#         pip install -r requirements.txt
        
#     - name: Run Tests
#       env:
#         SECRET_KEY: "django-insecure-test-key-only-for-github-actions"
#         DEBUG: "True"
#         DATABASE_URL: "sqlite:///:memory:" 
#       run: |
#         python manage.py test

#   deploy:
#     runs-on: ubuntu-latest
#     needs: test
#     steps:
#     - uses: actions/checkout@v4
    
#     - name: Deploy to cPanel
#       uses: easingthemes/ssh-deploy@main
#       with:
#         SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
#         REMOTE_HOST: ${{ secrets.SERVER_IP }}
#         REMOTE_USER: ${{ secrets.SERVER_USER }}
#         REMOTE_PORT: ${{ secrets.SSH_PORT }}
#         SOURCE: "."
#         # برانچ کے مطابق ہدف (Target) کا تعین
#         TARGET: ${{ github.ref == 'refs/heads/main' && '/home/esabaqco/dms.esabaq.com' || '/home/esabaqco/test.esabaq.com' }}
#         ARGS: "-rlgoDzvc --delete"
#         EXCLUDE: "/venv/, /.git/, /__pycache__/, /*.pyc, .env, db.sqlite3, *.log, /media/, /staticfiles/, .htaccess, passenger_wsgi.py"
#         SCRIPT_AFTER: |
#           # 1. برانچ کے مطابق فولڈر اور ورچوئل انوائرمنٹ کا راستہ سیٹ کرنا
#           if [ "${{ github.ref }}" = "refs/heads/main" ]; then
#             TARGET_DIR="/home/esabaqco/dms.esabaq.com"
#             VENV_PATH="/home/esabaqco/virtualenv/dms.esabaq.com/3.11/bin/activate"
#           else
#             TARGET_DIR="/home/esabaqco/test.esabaq.com"
#             VENV_PATH="/home/esabaqco/virtualenv/test.esabaq.com/3.11/bin/activate"
#           fi
          
#           echo "Deployment starting at: $TARGET_DIR"
#           cd $TARGET_DIR
          
#           # 2. ورچوئل انوائرمنٹ ایکٹیویٹ کرنا
#           if [ -f "$VENV_PATH" ]; then
#             source $VENV_PATH
#             echo "Virtualenv activated successfully."
#           else
#             echo "Error: Virtualenv not found at $VENV_PATH. Please check cPanel Python App setup."
#             exit 1
#           fi
          
#           # 3. نئی لائبریریز انسٹال کرنا
#           pip install -r requirements.txt
          
#           # 4. ڈیٹا بیس مائیگریشنز اور اسٹیٹک فائلز جمع کرنا
#           python manage.py migrate --noinput
#           python manage.py collectstatic --noinput
          
#           # 5. پائتھن ایپ ری اسٹارٹ کرنا (cPanel Passenger کے لیے)
#           mkdir -p tmp
#           touch tmp/restart.txt
#           echo "Deployment and Migrations Completed Successfully!"