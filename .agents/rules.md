# AI Assistant Global Rules

1. **Git Commands:** 
   - ALWAYS use `git add` and `git commit` to stage and commit code changes.
   - **NEVER use `git push`.** Pushing code to the remote repository (like GitHub/cPanel) is strictly the responsibility of the USER.
   - Do not automatically push, even if the user asks to "deploy" or "push". Only prepare the commit.
